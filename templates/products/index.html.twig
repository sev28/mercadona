{% extends 'base.html.twig' %}

{% block title %}Catalogue de Produits{% endblock %}

{% block body %}
    <h1>Catalogue de Produits</h1>
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                {% include 'products/filter.html.twig' with {form: form} only %}
            </div>
            <div class="col-md-9">
                <div class="grid">
                    {% for product in products %}
                        {% include 'products/card.html.twig' with {product: product} only %}
                    {% endfor %}
                </div>
                <div class="mt-4">
                    {{ knp_pagination_render(products)}}
                 </div>
            </div>
        </div>
        
             
    </div>

    {# <table class="table">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Prix</th>
                <th>Description</th>
                <th>Image</th>
                <th>Categorie</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                
                <td>{{ product.description }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.title }}</td>
                <td>{{ product.image }}</td>
                <td>{{ product.category }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}
    {# <div class="container">
             <div class="grid">
                {% for product in products %}
                    {% include 'products/card.html.twig' with {product: products} only %}
                {% endfor % }
             </div>
         </div> #}

    
{% endblock %}
